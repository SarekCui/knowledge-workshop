# 架构决策记录（ADR）

涉及以下变化时必须新增 ADR：服务拆分、数据所有权、中间件选型、一致性策略、对外契约破坏性变更、安全模型和关键版本升级。

文件命名：`NNNN-short-title.md`，编号递增且已接受的记录不复写历史；决策变化时新建 ADR 并标记旧记录被替代。

## 模板

```markdown
# ADR-NNNN：标题

- 状态：提议 / 已接受 / 已替代 / 已拒绝
- 日期：YYYY-MM-DD
- 决策人：

## 背景

## 决策

## 备选方案

## 影响

### 正面影响

### 代价与风险

## 验证方式
```
