# 第5.6轮学习端验证（核心播放链路已验证，轮次仍进行中）

- 日期：2026-09-14
- 环境：macOS、Node20.17.0、React18.3.1、AntDesign5.27.6、Vite6.4.3、Vitest4.1.11。
- 当前变更未提交，不能提供新的 Git commit。

## 已验证

- TypeScript严格编译与生产构建通过。
- 前端测试覆盖连续播放区间、拖动排除、原事件重试、409会话冲突与有界队列背压。
- 官方npm完整审计无已知漏洞。
- 隔离浏览器加载真实5173页面；账号、密码与登录按钮可见，无Vite错误覆盖层，浏览器`errors`无输出。
- 本地演示账号通过真实网关/IAM登录，令牌没有打印或存储到localStorage。
- 页面无Mock数据或虚构课程；Context/Hook提供共享状态和方法，播放器逻辑与展示分离。

## 阻塞与处理

- 旧Docker数据卷缺少knowledge_learning库；已按已有初始化SQL补建，仅由Flyway执行表迁移，未清空旧库。
- 学习服务启动失败：旧`learning.group-formed`队列无死信参数，与代码声明不一致；网关课程请求因此503，前端显示实际失败。
- 只读检查显示该队列仍有2条待消费消息、0条未确认消息，不能直接删除重建。
- 未删除队列、未绕过消费者或替换虚假接口。需要安全迁移旧队列后继续课程/视频真实验收。
- 尚未验证真实视频播放、MySQL落库后重开恢复、暂停/拖动和跨设备完整链路，不标记5.6完成。
- 内存令牌与进度队列不支持重载恢复；HLS适配、刷新令牌UI、持久化离线队列未实现。
- AntDesign`use client`提示与首屏包大小警告尚存，打包性能尚未验收。

## 复现

```bash
cd knowledge-web
npm ci --registry=https://registry.npmjs.org
npm test
npm run build
npm audit --registry=https://registry.npmjs.org
npm run dev
```

开发页面：http://127.0.0.1:5173；代理网关：http://127.0.0.1:8080。

## 同日修复跟进

- 营销和学习改用`learning.group-formed.v2`，旧队列不删除，事件路由键保持不变。
- 使用确认转发工具迁移2条旧消息：sourceRemaining=0、targetReady=2；未purge队列。
- 学习服务健康检查恢复UP；消息最终消费/死信状态另外核查，不把迁移等同于发放权益。
- 课程查询状态独立区分idle/loading/success/error，接口失败显示“课程加载失败”与重试，只有成功空结果才显示“暂无有效课程权益”。
- 前端7项测试与生产构建通过；学习/营销容器验收通过，新增旧参数队列与新队列共存回归场景。
- 操作说明见`docs/10-rabbitmq-queue-migration.md`。完整视频播放与断点端到端验收仍未完成。
- 真实浏览器复验登录、本人课程查询成功，页面无错误提示、无Vite覆盖层、`errors`无输出。
- 当前学习库课程0条、权益0条，空列表与数据库事实一致，没有初始化虚假课程或强行发放权益。
- 最终消息状态：旧队列0、新队列0、成团死信队列2、未确认消息均0。两条历史消息仍需独立排查消费兼容/业务失败，不宣称它们已成功发放权益。
- 本次容器验收：学习13项、营销11项，错误/失败/跳过均0；前端7项测试通过。

## 10秒续播同步跟进

- 采用用户确认的10秒心跳、暂停/拖动结束/结束立即提交，心跳位置未变化且无新增区间时跳过。不新增本地每秒持久化，不修改现有完成率算法。
- 先新增Hook事件/定时器回归：旧实现4项中3项失败，分别为10秒心跳、拖动暂态位置、恢复定位暂态位置；修改后通过。额外覆盖结束、隐藏/卸载和心跳清理。
- 前端13项测试通过，TypeScript严格检查和生产构建通过；AntDesign指令及包大小提示仍存在。使用React最佳实践检查，高频变量留在Effect/ref，不新增组件参数传递，事件和定时器清理保持成对。
- `JAVA_HOME=...jdk-17... mvn -Dmaven.repo.local=.m2/repository -pl knowledge-learning -am test -q`退出0；此命令不代表本轮重新执行全部Testcontainers验收。
- 测试课程`4894c891-7e95-41d9-9f2f-441018762562`，视频`video-oceans-local-v1`；真实管理API创建，demo与专用学习者模拟支付成团，数据库团状态FORMED、人数2/2、权益2条。重复执行测试数据脚本复用清单/支付流水成功。
- 独立浏览器账号`local-test-peer`：记录心跳客户端时间09:36:16.880Z、09:36:26.869Z、09:36:36.864Z，间隔约10秒，HTTP200；这些时间是观测证据，不是强实时承诺。
- 实际播放暂停30.171405秒，数据库resume_position_ms=30171；重新点击打开视频创建新会话，接口resumePositionMs=30171，等待seeked后播放器30.171秒。再回退到12.345秒，数据库resume_position_ms=12345、max_position_ms=30171。
- 浏览器`errors`无输出。初始恢复定位等待seeked完成，期间不提交暂停/拖动暂态位置；重复metadata不重新定位。
- 早先demo浏览器22.381秒与共享账号后端进度不一致的观察，不足以单独证明后端缓存竞争根因。本次隔离账号核心续播链路通过，不将该观察表述成已定位的缓存缺陷。
- 尚未完成跨设备并发浏览器、强杀退出、持久化离线队列、HLS及完整观看率验收；退出最终事件只能尽力发送，5.6不标记整体完成。
