# 第 5.6 轮：技术 Note 图片验收

## 自动化结果

- `knowledge-learning`：新增 7 个图片单元场景，覆盖 JPEG/PNG 重编码、尺寸压缩、伪造类型拒绝、Markdown 引用去重、代码示例忽略、跨用户拒绝和公开读取授权；相关测试通过。
- `LearningAcceptanceIT`：18 个真实 MySQL、Redis、RabbitMQ 容器场景通过，无失败、无跳过。Flyway V7、图片绑定/释放、跨用户事务回滚、到期认领和静态注解 SQL 均由真实 MySQL 验证。
- 前端：22 个测试文件、85 个测试通过；新增 FormData 上传、类型/大小前置校验和站内稳定图片地址测试。
- Maven 全模块跳过测试打包、TypeScript 检查和 Vite 生产构建通过。现有主包约 1.53MB（gzip 约 484KB），保留既有大块警告，代码拆分不混入本功能批次。

## 真实浏览器结果

本地完整服务和 MinIO 启动后，使用 `demo` 演示账号完成：

1. 新建独立技术 Note，添加 `MinIO` 标签并上传 PNG；
2. 服务端将测试图重编码为 1600×827，正文插入稳定图片 ID；
3. 保存后数据库图片记录由 `TEMP` 转为 `BOUND`，`note_id` 与 Note 一致；
4. 发布 Note 后打开公开详情，页面通过授权接口取得 1 小时签名地址并正确展示私有 Bucket 图片；
5. 页面控制台无错误或警告，服务启动后的图片清理作业无 SQL 异常。

验收 Note ID：`3f8cb3a5-7b25-42f3-8349-606c9ff8f464`；图片 ID：`67a143c6-600f-4d6e-a1e6-41e0cf72f149`。本地项目保持运行：`http://127.0.0.1:5173/`。
