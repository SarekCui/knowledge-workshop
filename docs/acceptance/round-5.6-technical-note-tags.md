# 第 5.6 轮：技术 Note 标签验收

## 自动化结果

- 后端相关 Reactor：19 个单元测试通过，无失败、无跳过；新增标签归一、非法输入和有序替换测试。
- `LearningAcceptanceIT`：17 个真实 MySQL、Redis、RabbitMQ 容器场景通过，无失败、无跳过。
- Flyway V6 在 MySQL 8.4 空库成功执行；验收覆盖标签创建、大小写无关去重、更新、详情回读、关键词命中标签、精确标签筛选和幂等参数冲突。
- 前端：21 个测试文件、82 个测试通过；TypeScript 检查与 Vite 生产构建通过。现有主包约 1.53 MB（gzip 约 483 KB），仍有 Vite 大块警告，代码拆分作为独立性能批次处理。

## 浏览器结果

本地五个服务和 Vite 重启后，使用真实浏览器完成：

1. 打开已有 Markdown 技术 Note，手动录入 `Java`、`Redisson` 并保存；
2. 本人列表正确展示两个标签，发布成功；
3. 发现页卡片展示可点击标签；
4. 点击 `#Java` 后只返回精确匹配笔记，并显示可清除的当前筛选条件。

验收使用的 Note ID 为 `6ac2761f-41e4-4f3d-a831-3e61118d7c5b`。本地项目保持运行：`http://127.0.0.1:5173/`。
